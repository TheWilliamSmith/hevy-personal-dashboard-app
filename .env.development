# Base URL the app prefixes to every API path.
# In dev this stays relative so requests go through the Vite proxy (see vite.config.ts).
VITE_API_URL=/api

# Where the dev proxy forwards /api. Only read by vite.config.ts.
VITE_API_PROXY_TARGET=http://localhost:3000
